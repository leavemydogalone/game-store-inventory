//- maybe just use the game list

extends layout 

block content 
  h1= title

  ul 
    each game in game_list
      li 
        a(href=game.url) #{game.name}
       
        //- p= game.year.name 
        p Copies in stock: #{game.stock}
    else  
      li There are no categories
        