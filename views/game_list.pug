extends layout 

block content 
  h1= title

  ul 
    each game in game_list
      li 
        a(href=game.url) #{game.name}
        p 
          a(href=game.designer.url) #{game.designer.name}
          p 
            a(href= game.year.url) #{game.year.name}
            p Copies in stock: #{game.stock}
    else  
      li There are no categories
        